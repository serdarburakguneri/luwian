<!-- Example JSON logging fragment (not auto-applied). Users may copy/reference it.
     LuwianJsonLoggingConfig will later switch to a JSON encoder programmatically. -->
<configuration>
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
  </appender>
  <root level="INFO">
    <appender-ref ref="STDOUT"/>
  </root>
</configuration>
